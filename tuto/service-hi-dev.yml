eureka:
  client:
    serviceUrl:
      #defaultZone: http://localhost:8761/eureka/
      defaultZone: ${eureka.service.uri:http://m.hello-game.net:1111}/eureka/

server:
  port: ${config.port:8762}
spring:
  application:
    name: service-hi

# Dynamic config values
#   eureka.service.uri
#   config.port
